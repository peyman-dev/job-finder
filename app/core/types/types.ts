import React from "react";

export type PopularVacancy = {
  title: string;
  value: number;
};

export interface HowItWorkStepType {
    title: String,
    description: String,
    icon: React.CElement<any, any>
}
